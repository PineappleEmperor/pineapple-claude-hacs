# Test-only dependencies. Installed by .github/workflows/python_validate.yml
# before the pytest step; also `pip install -r requirements.test.txt` locally.
#
# pytest-homeassistant-custom-component tracks Home Assistant releases 1:1 and
# hard-pins `homeassistant==<that release>`, so the pin below decides which HA
# version the suite runs against. A mismatched pin fails at import, not at test
# time. Pick the release matching the HA you target, and keep its Python floor
# in lockstep with `python-version` in python_validate.yml (a single value —
# a matrix renames the required check-run), `target-version` in pyproject.toml,
# and pyrightconfig.json. scripts/version_sync.py compares those three.
#
# Version map (verify at pypi.org/project/pytest-homeassistant-custom-component):
#   0.13.357 -> homeassistant 2026.8.3, requires-python >=3.14
pytest-homeassistant-custom-component==0.13.357


# Add integration-specific test deps below (e.g. a fake transport, respx,
# freezegun beyond what the plugin already pins).

# --- Panel integrations only -------------------------------------------------
# If the integration registers a panel it declares `frontend` (and usually
# `panel_custom`) in manifest.json `dependencies`. The frontend COMPONENT has its
# own pip requirement, and `pip install homeassistant` does NOT pull it in —
# component requirements are installed by HA at runtime, not by pip.
#
# Without it EVERY setup test fails:
#   ERROR: Error during setup of component frontend: No module named 'hass_frontend'
#   DependencyError: Could not setup dependencies: frontend, panel_custom
#
# The worst property: it usually passes LOCALLY, because a dev machine already has
# the package from some earlier install. Local green, CI red — and the failures
# surface as `'MockConfigEntry' object has no attribute 'runtime_data'`, which
# points at the integration rather than at a missing dependency.
#
# Pin it from core's own manifest for the HA version pinned above, NOT from PyPI latest —
# PyPI is routinely ahead, and a mismatch reintroduces the same import failure:
#   curl -s https://raw.githubusercontent.com/home-assistant/core/<ha-version>/homeassistant/components/frontend/manifest.json
# Re-derive it whenever the pytest-homeassistant-custom-component pin moves.
# home-assistant-frontend==<version from that manifest>   # uncomment for panel integrations
